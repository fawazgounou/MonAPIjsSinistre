class Users {
    constructor( name, mail, password,role ) {
     
            this.name = name;
            this.mail = mail;
            this.password = password;
            this.role = role;
        }
}

export default Users;