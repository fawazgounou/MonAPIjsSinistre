class Users {
    constructor( id,name, mail,role ) {
                   this.id = id;
                   this.name = name;
                    this.mail = mail;
                    this.role = role;
        }
}

module.exports= Users;